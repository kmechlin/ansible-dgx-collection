---
- name: k3s with NVIDIA runtime (opt-in via k3s_install)
  hosts: dgx
  become: true
  gather_facts: true
  roles:
    - role: kmechlin.dgx.k3s_gpu
      when: k3s_install | default(false) | bool
